/** 1 - The 3 Primitives */

// DEMO: The 3 primitives (typed, inferred)
const age = 15
const isSingle: boolean = true
const name: string = "Poom"

// DEMO: Result from invoking functions
const value = Math.sqrt(8)
const names = "Poom".repeat(10)

// DEMO: Operators (Arithmetic, Boolean Logic, Bitwise)
const n = 10 * 20 + 30 / 40
const b = (true && false) || !true
const c = 20 << 5

/**
 * TODO(4)
 *
 * Create a variable for
 * - Donald Trump's Age
 * - Say Your Name 600 times
 *   Hint: string has a "repeat" method
 * - Your birth date, times,your birth month, times your birth year.
 * - (add more here on operators)
 */
